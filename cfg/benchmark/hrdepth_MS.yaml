# -----------------------------------------------------------------------------
net:
  depth:
    dec_name: 'hrdepth'
# -----------------------------------------------------------------------------
loss:
  img_recon:
    use_min: True
    use_automask: True
# -----------------------------------------------------------------------------
dataset:
  supp_idxs: [ -1, 1, 0 ]
# -----------------------------------------------------------------------------
