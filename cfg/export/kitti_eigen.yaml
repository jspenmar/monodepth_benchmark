# -----------------------------------------------------------------------------
dataset:
  type: 'kitti_lmdb'
  split: 'eigen'
  mode: 'test'
  supp_idxs: ~
  use_depth: False
# -----------------------------------------------------------------------------
